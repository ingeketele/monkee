var test = '<%= @activity.id %>'
var activityHeart = document.querySelectorAll(`#activity_${test}`);
activityHeart.forEach((heart) => {
  heart.innerHTML = '<%= j render "activities/heart", activity: @activity %>';
})
